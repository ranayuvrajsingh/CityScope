import React from 'react';
import './FeaturedExp.scss';

const FeaturedExp = (props) => {
    return (
        <div className="featuredexp">
            <div className='featuredexp__img'>
                <h2>this is featured exp below channels</h2>
            </div>
        </div>
    );
};

export default (FeaturedExp);